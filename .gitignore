# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Data (too big / regenerable — never commit raw data)
data/
# Raw data files (large, regenerable) — but allow results CSVs in notebooks/
data/*.csv
data/*.parquet
*.parquet
!notebooks/*.csv

# LLM cache
cache/

# Secrets
.env
*.key

# OS / editor
.DS_Store
.vscode/
